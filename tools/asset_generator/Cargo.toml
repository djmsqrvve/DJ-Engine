[package]
name = "asset_generator"
version = "0.1.0"
edition = "2021"
description = "MIDI generation and asset processing utility"
repository.workspace = true
license.workspace = true

[dependencies]
image = "0.24"
midly = { version = "0.5", features = ["std"] }
